# Normalize line endings
# Always use LF in the repository; Git auto-converts on checkout for Windows
* text=auto eol=lf

# Explicitly set binary files so Git never tries to diff or convert them
*.pdf      binary
*.exe      binary
*.traineddata binary
*.png      binary
*.jpg      binary
*.jpeg     binary
*.ico      binary
