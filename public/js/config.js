// ─── Auto-generated by scripts/build-frontend-config.js ───────────────────
// Commit this file with an empty API_BASE_URL for local dev.
// Before deploying to Vercel, run:
//   RENDER_URL=https://your-app.onrender.com node scripts/build-frontend-config.js
// then commit the updated file.

(function () {
  "use strict";

  var isLocalhost =
    window.location.hostname === "localhost" ||
    window.location.hostname === "127.0.0.1";

  // In local dev: same-origin (frontend served by Express on port 3000)
  // In production: Vercel frontend calls Render backend at a different URL
  window.API_BASE_URL = isLocalhost
    ? "" // same-origin — no prefix needed
    : ""; // ← Replace with your Render URL after deploying the backend
           //   e.g. "https://pdf-footer-scanner-api.onrender.com"
           //   Then run: RENDER_URL=https://... node scripts/build-frontend-config.js

  if (!isLocalhost && !window.API_BASE_URL) {
    console.warn(
      "[PDF Scanner] Production API URL not configured.\n" +
      "Run: RENDER_URL=https://your-app.onrender.com node scripts/build-frontend-config.js"
    );
  }
})();
